package logic;

import objects.*;

/**
 *
 * @author Joann
 */
public interface ILogic {
    /**
     * Searches for lists and titles in ppt and converts them
     * @param ppt 
     */
    void format(PPT ppt);
}
